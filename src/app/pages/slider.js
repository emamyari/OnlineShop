import React from "react";
import {useSubheader} from "../../_metronic/layout";

export const Slider = () => {
  const suhbeader = useSubheader();
  suhbeader.setTitle("Slider");

  return (<>Slider</>);
};
